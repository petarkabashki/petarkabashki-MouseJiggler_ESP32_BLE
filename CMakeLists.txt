# ESP-IDF build (framework = espidf). The Arduino environments do not use this
# file; they are driven entirely by platformio.ini.
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(mousejiggler-forth)
